import { MetadataRoute } from 'next';
// Import trực tiếp file JSON (đảm bảo database.json nằm ở thư mục gốc, ngang hàng với thư mục app)
import articles from '../database.json';

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = 'https://ninebot.vn';

  // 1. Khai báo các trang tĩnh cố định
  const staticRoutes: MetadataRoute.Sitemap = [
    {
      url: `${baseUrl}`,
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 1,
    },
    {
      url: `${baseUrl}/ky-thuat`,
      lastModified: new Date(),
      changeFrequency: 'daily',
      priority: 0.8,
    },
    {
      url: `${baseUrl}/tin-tuc`,
      lastModified: new Date(),
      changeFrequency: 'daily',
      priority: 0.8,
    },
  ];

  // 2. Tự động sinh URL cho từng bài viết kỹ thuật từ file JSON
  const dynamicRoutes: MetadataRoute.Sitemap = articles.map((article: any) => ({
    url: `${baseUrl}/ky-thuat/${article.id}`,
    lastModified: new Date(),
    changeFrequency: 'monthly',
    priority: 0.6,
  }));

  // Gộp tất cả lại đẩy ra cho Google
  return [...staticRoutes, ...dynamicRoutes];
}